name: Debug

on:
  workflow_dispatch:

jobs:
  Debug:
    runs-on: ubuntu-latest
    steps:
      - name: Try
        run: |
          mkdir z1
          mkdir z2
          mkdir z3
          ls
          ls > zeko.txt
          cat zeko.txt
