name: Build and Test

on: [push]

jobs:
  Run-main:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Build and Test
        uses: actions/setup-node@v3
        with:
          node-version: 16.8.0
      - run: |
          npm i
          npm run test
